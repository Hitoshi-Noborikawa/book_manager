<div class="card">
  <div class="card-header bg-primary text-white">
    <h2><%= @book.title %></h2>
  </div>
  <div class="card-body">
    <%= turbo_frame_tag "book-show" do %>
      <%= render "content", book: @book %>
    <% end %>
  </div>
</div>

<div class="mt-3">
  <%= link_to '一覧に戻る', books_path %>
</div>
