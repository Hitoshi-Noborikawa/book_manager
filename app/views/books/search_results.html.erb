<h1>検索結果</h1>

<% if @book_info.present? %>
  <p>タイトル: <%= @book_info[:title] %></p>
  <p>著者: <%= @book_info[:author] %></p>
  <% if @book_info[:cover_url] %>
    <img src="<%= @book_info[:cover_url] %>" alt="表紙">
  <% end %>
  <%= link_to "この本を登録する", new_book_path(
    title: @book_info[:title],
    author: @book_info[:author],
    cover_url: @book_info[:cover_url]
  ) %>
<% else %>
  <p>本が見つかりませんでした。</p>
<% end %>

<%= link_to "もう一度検索する", search_books_path %>
